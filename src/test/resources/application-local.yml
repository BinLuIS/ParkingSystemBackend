logging.file: logs/test.log
logging:
  level:
    org:
      springframework:
        web: OFF
      hibernate: OFF
    root: OFF
spring:
  datasource:
    url:  jdbc:h2:mem:testdb
  jpa:
    show-sql: true
  h2:
    console:
      enabled: true
